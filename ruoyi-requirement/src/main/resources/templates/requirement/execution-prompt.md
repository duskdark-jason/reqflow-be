# 执行提示词

请基于 ${demandNo} ${demandTitle} 在需求设计阶段创建的任务分支 ${taskBranch} 完成开发。

执行要求：

1. 先确认当前仓库远端、基线分支和任务分支与需求平台资料包一致。
2. 只沿用 ${taskBranch}，不要在开发阶段重新生成不同任务分支。
3. 基于最终 `requirement.md` 生成或更新 `plan.md`，再开始实现。
4. 执行、补验或返修后持续更新同一个 `execution-report.md`，并通过 MCP 回写执行报告新版本。
