# Review 提示词

请审查 ${demandNo} ${demandTitle} 的实现，重点关注需求符合度、回归风险、权限、SQL 和验证证据。

Review 要求：

1. 只读审查，不直接改代码。
2. 在同一个任务分支和 spec 目录更新 `review-report.md`。
3. 发现返修项时使用 `RF-*` 编号，并交回 Execution Agent 更新 `execution-report.md`。
4. 返修复审后继续补充同一个 `review-report.md`，并通过 MCP 回写 Review 报告新版本。
